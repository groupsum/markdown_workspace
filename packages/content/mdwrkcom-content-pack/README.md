# @mdwrk/mdwrkcom-content-pack

This package moved out of `markdown_workspace`.

The source-of-truth home for `@mdwrk/mdwrkcom-content-pack` is now the `mdwrkcom` repository at:

- `E:\swarmauri_github\mdwrkcom\packages\content\mdwrkcom-content-pack`

The package name stays the same on npm. Consumers should keep installing:

```bash
npm install @mdwrk/mdwrkcom-content-pack
```

This directory is no longer the maintained package source.
