version: "3.9"

services:
  client:
    container_name: markdown_workspace_client
    build:
      context: .
      dockerfile: client/Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - dockerwp

networks:
  dockerwp:
    name: dockerwp
