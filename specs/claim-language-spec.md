# Claim Language Lint Spec

Certification-strength language must remain explicitly qualified unless complete closure evidence exists.

When this combined phrase appears:

- `certifiably fully featured and certifiably fully markdown spec compliant`

it must include a qualifier such as `not yet` on the same line.

Automated validator: `tools/governance/lint-claim-language.mjs`.
