name: Lander Article Metadata

on:
  push:
    branches:
      - master
    paths:
      - apps/lander/data/article-metadata.schema.json
      - apps/lander/data/markdown/blog/**
      - apps/lander/data/markdown/docs/**
      - apps/lander/package.json
      - apps/lander/scripts/validate-article-metadata.mjs
      - .github/workflows/lander-article-metadata.yml
  pull_request:
    paths:
      - apps/lander/data/article-metadata.schema.json
      - apps/lander/data/markdown/blog/**
      - apps/lander/data/markdown/docs/**
      - apps/lander/package.json
      - apps/lander/scripts/validate-article-metadata.mjs
      - .github/workflows/lander-article-metadata.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lander-article-metadata-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate article metadata headers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm run ci:install

      - name: Validate docs and blog metadata schema
        run: npm run validate:article-metadata -w apps/lander
